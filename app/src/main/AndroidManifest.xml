<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daniel.economia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Economia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Futurop" />
        <activity android:name=".Activities.Presentef" />
        <activity android:name=".Activities.Presentea" />
        <activity android:name=".Activities.Anualp" />
        <activity android:name=".Activities.Anualf" />
        <activity android:name=".Activities.Futuroa"></activity>
    </application>

</manifest>